# File-organiser-Application
A simple Python file-organizer that scans the current directory, filters files by extension, auto-renames them sequentially, and moves them into a dedicated folder (like images/) for clean and structured storage.
